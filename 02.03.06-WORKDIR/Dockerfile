# Минималистичный образ busybox версии 1.37.0.
FROM busybox:1.37.0
# Объявление переменной окружения SUBDIR со значением DIR_B.
ENV SUBDIR=DIR_B
# Смена рабочего каталога на DIR_A
WORKDIR DIR_A
# Смена рабочего каталога на подкаталог,
# указанный в переменной окружения SUBDIR.
WORKDIR $SUBDIR
# Создание текстового файла с датой и временем создания
# в имени.
RUN touch $( date '+%Y-%m-%d_%H-%M-%S' )
# Просто вывод списка файлов, чтобы убедиться,
# что файл действительно был создан.
RUN ls
