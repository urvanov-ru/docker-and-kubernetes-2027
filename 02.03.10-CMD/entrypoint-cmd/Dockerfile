# Базовый образ busybox версии 1.37.0
FROM busybox:1.37.0
# Выводим сообщение при запуске контейнера
ENTRYPOINT ["echo", "Hello, "]
# Команды из инструкции CMD можно переопределить
# при запуске контейнера.
CMD ["World!"]